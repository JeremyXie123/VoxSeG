# Installation
1. Create and activate a new conda environment with: `python=3.11`
2. Install dependencies: `python -m pip install -r requirements.txt`
3. Run files: `python visualize_splat.py`

# Notes
1. Models should go in `models/*.obj`
2. Splats should go in `splats/*.ply`